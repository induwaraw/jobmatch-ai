# ML workspace

Training and experimentation for JobMatch AI. Nothing here is installed into the
backend virtual environment. Models are trained separately or in Google Colab,
and only the resulting artefacts and predictions are used by the API.

Folders:

- `notebooks/` exploratory work and training notebooks
- `models/` trained model files, ignored by git

See `requirements.txt` for the libraries this workspace needs.
