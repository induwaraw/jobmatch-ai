# JobMatch AI - ML workspace dependencies (Python 3.10, ml/.venv).
#
# These are the light ones only, for data prep and classical models that run
# on this machine. The heavy training libraries are Colab-only and are
# deliberately NOT listed here: torch, tensorflow, transformers,
# sentence-transformers, spacy, prophet. Do not install those locally, and
# never into backend/.venv.
#
#   py -3.10 -m venv ml\.venv
#   ml\.venv\Scripts\Activate.ps1
#   pip install -r ml\requirements.txt

pandas==2.3.3
scikit-learn==1.7.2
kaggle==1.7.4.5
python-dotenv==1.2.3
